Simple react-native app which uses the API of The Movie Database to search movies.

# Build

`yarn start`

and then  
`yarn android` for Android  
or  
`yarn ios` for iOS.  

# Test

`yarn test`

# Flow

`yarn flow`

# Eslint

`yarn lint`
