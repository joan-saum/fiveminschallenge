import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    marginLeft: 20,
    marginRight: 20,
    marginBottom: 20,
    paddingLeft: 10,
    paddingRight: 10,
    borderWidth: 1,
  },
  input: {
    alignItems: 'center',
    // borderWidth: 1,
    flexGrow: 1,
    // height: 35,
    color: 'black',
  },
});

export default style;
