// @flow

import React from 'react';
import {Text} from 'react-native';

import type {Node} from 'react';

type Props = {||};

const Mylist = ({}: Props): Node => {
  return <Text>MYLIST</Text>;
};

export default Mylist;
